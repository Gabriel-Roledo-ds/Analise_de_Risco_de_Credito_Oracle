# Credit Risk Analytics — Dependências Python
# Testadas no OCI Data Science (VM.Standard.E2.1.Micro · 1 OCPU · 1 GB RAM)
# Instalação: pip install -r requirements.txt

# ── Conexão com OCI ──────────────────────────────────────────
oci>=2.100.0                  # OCI SDK — Object Storage, Data Science, etc.
cx_Oracle>=8.3.0              # Conexão Python → Oracle ATP

# ── Manipulação de dados ─────────────────────────────────────
pandas>=1.5.0,<2.1.0          # Versão testada no OCI Data Science
numpy>=1.23.0,<1.25.0         # Compatível com scikit-learn abaixo

# ── Machine Learning ─────────────────────────────────────────
scikit-learn>=1.1.0,<1.4.0
lightgbm>=3.3.5               # Modelo principal — roda no 1GB RAM
optuna>=3.0.0                 # Tuning de hiperparâmetros (opcional)

# ── Visualização ─────────────────────────────────────────────
matplotlib>=3.6.0
seaborn>=0.12.0

# ── Métricas de crédito ──────────────────────────────────────
scipy>=1.9.0                  # KS test (scipy.stats.ks_2samp)

# ── Utilitários ──────────────────────────────────────────────
python-dotenv>=0.21.0         # Gerenciar variáveis de ambiente
joblib>=1.2.0                 # Serialização de modelos
tqdm>=4.64.0                  # Progress bars em notebooks
